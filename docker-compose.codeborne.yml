# For demo deployment to arti.codeborne.com
services:
  tracker:
    volumes:
      - attachments:/app/attachments
    networks:
      - proxy
      - default

  db:
    volumes:
      - ./db/init:/docker-entrypoint-initdb.d

networks:
  proxy:
    external: true

volumes:
  attachments:
